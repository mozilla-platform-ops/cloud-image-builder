name: Manual Deploy
on: [workflow_dispatch]

jobs:
    build:
      name: Run Script
      runs-on: windows-latest
      steps:
        - uses: actions/checkout@v1
        - name: Script
          run: .\scripts\purge-deprecated-azure-resources.ps1
          shell: powershell